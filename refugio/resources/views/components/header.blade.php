<header class="main-header">
    <div class="logo">El Refugio</div>
    <nav class="nav-links">
        <a href="{{ route('home') }}">Inicio</a>
        <a href="{{ route('public.user.login') }}">Login</a>
        <a href="{{ route('public.user.register') }}">Registro</a>
    </nav>
</header>
